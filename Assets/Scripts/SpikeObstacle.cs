using UnityEngine;
using System.Collections;

public class SpikeObstacle : MonoBehaviour {
	/*
	void OnTriggerEvent2D(Collider2D otherObject){
		if (otherObject.tag == "Player") {
			Character player = (Character) otherObject;
			if(player.activePlayer){
				userCrashed();
			}
		}
	}

	private void userCrashed(){
		ApplicationModel.lifesLeft
		Application.LoadLevel (0);
	}*/
}
